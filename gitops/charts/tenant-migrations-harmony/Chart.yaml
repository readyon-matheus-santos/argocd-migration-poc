apiVersion: v2
name: tenant-migrations-harmony
description: What ships to readyon-harmony-charts as readyon-helm-charts/tenant-migrations - the tenant's migration Jobs, deployed on the tenant cluster by the parent's PreSync hook Application. One Job per migrating service, plain resources named per release, sync-waves serialize services that share a database
type: application
version: 0.1.0
appVersion: "0.1.0"
