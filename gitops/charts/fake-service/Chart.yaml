apiVersion: v2
name: fake-service
description: Stand-in service Deployment (alpine + VERSION/RELEASE_ID) with an optional per-app PreSync migration Job, used by the baseline scenario
type: application
version: 0.1.0
appVersion: "0.1.0"
